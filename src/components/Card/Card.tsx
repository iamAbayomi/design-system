import styled from 'styled-components'

type props ={
    children: ""
}

const Card: React.FC<props> = (props) => {
    return(
        <div>
            <CardBody>


            </CardBody>
        </div>
    )
}

export default Card


const CardBody = styled.div`
`